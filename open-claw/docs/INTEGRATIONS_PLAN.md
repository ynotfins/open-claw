# Archived — Integrations Plan

This document has been superseded and kept only as an archive pointer.

Archived historical copy:

- `open-claw/docs/archive/INTEGRATIONS_PLAN-2026-02-18.md`

Use these files instead:

- `open-claw/docs/INTEGRATIONS.md` for current integration targets
- `open-claw/docs/BLOCKED_ITEMS.md` for current wrapper-specific unblock steps
- `vendor/openclaw/docs/*` for upstream-supported runtime and channel behavior

Reason for archival:

- the original plan mixed upstream OpenClaw capabilities with wrapper-specific policy choices
- it promoted WhatsApp as a `P0` integration while newer wrapper docs treat Meta Business setup as optional and blocked
- it was no longer the safest canonical reference for current implementation decisions
